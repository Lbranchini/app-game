"""Infrastructure layer: concrete implementations of the application ports.

May depend on `domain` and `application`. Interfaces depend on this layer
through the ports defined in `application/ports.py`.
"""
